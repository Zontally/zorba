{
  "name": "@zontally/zorba",
  "version": "0.3.0",
  "description": "ZORBA — Zontally Reference Business Architecture. The open reference architecture for the agentic enterprise.",
  "license": "SEE LICENSE",
  "repository": {
    "type": "git",
    "url": "https://github.com/Zontally/zorba.git"
  },
  "scripts": {
    "validate": "node build/validate.js",
    "compile": "node build/compile.js --all",
    "compile:industries": "node build/compile-industries.js",
    "generate": "node build/generate-docs.js",
    "build": "npm run validate && npm run compile && npm run compile:industries && npm run generate",
    "version": "echo $npm_package_version"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  }
}
